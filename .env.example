###############################################
# Environment template for local development  #
# Copy to .env.local and fill real values     #
###############################################

# NOTE: Only variables prefixed with VITE_ are exposed to the browser by Vite.
# .env.local is gitignored. Never commit real secrets.

# Supabase client config (browser-safe, but still sensitive)
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR-ANON-KEY.PART

# Optional dev-only auto sign-in used by DevAuthGate (leave empty to disable)
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=

# Server-side only (used by local scripts like scripts/seed.cjs)
# These are NOT exposed to the browser; do not use these in client code.
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE=YOUR-SERVICE-ROLE-KEY

# Security best practices
# - Use different keys for dev and prod; never reuse prod keys locally.
# - Keep .env.local out of version control (already covered by .gitignore).
# - Enforce RLS on all tables and prefer least-privilege policies.
# - Rotate keys on suspicion of leakage; invalidate old tokens.
# - For deployment, inject env vars at runtime via platform secrets, not in repo.
